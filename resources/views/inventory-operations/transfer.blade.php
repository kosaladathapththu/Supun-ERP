@extends('layouts.app') @section('title','Stock Transfer') @section('content')
<a href="{{ route('inventory-operations.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Inventory operations</a><h1 class="h3 page-title mt-2">Location Transfer</h1><form method="POST" action="{{ route('inventory-operations.transfer.store') }}">@csrf<div class="card mb-4"><div class="card-body"><div class="row g-3"><div class="col-md-4"><label class="form-label">From</label><select name="from_location_id" class="form-select">@foreach($locations as $l)<option value="{{ $l->id }}">{{ $l->code }} — {{ $l->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">To</label><select name="to_location_id" class="form-select">@foreach($locations as $l)<option value="{{ $l->id }}">{{ $l->code }} — {{ $l->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Notes</label><input name="notes" class="form-control"></div></div></div></div>@include('inventory-operations.partials.items',['field'=>'quantity','signed'=>false])<button class="btn btn-primary mt-3">Post transfer</button></form>
@endsection
