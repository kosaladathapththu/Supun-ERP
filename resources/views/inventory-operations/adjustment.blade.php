@extends('layouts.app') @section('title','Stock Adjustment') @section('content')
<a href="{{ route('inventory-operations.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Inventory operations</a><h1 class="h3 page-title mt-2">Stock Adjustment</h1><div class="alert alert-warning">Use negative quantities for shortages/damage and positive quantities for verified gains.</div><form method="POST" action="{{ route('inventory-operations.adjustment.store') }}">@csrf<div class="card mb-4"><div class="card-body"><div class="row g-3"><div class="col-md-4"><label class="form-label">Location</label><select name="stock_location_id" class="form-select">@foreach($locations as $l)<option value="{{ $l->id }}">{{ $l->code }} — {{ $l->name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Type</label><select name="adjustment_type" class="form-select"><option value="correction">Correction</option><option value="damaged">Damaged stock</option><option value="opening">Opening balance</option></select></div><div class="col-md-5"><label class="form-label">Reason</label><input name="reason" class="form-control" required></div></div></div></div>@include('inventory-operations.partials.items',['field'=>'quantity_change','signed'=>true])<button class="btn btn-warning mt-3">Post adjustment</button></form>
@endsection
