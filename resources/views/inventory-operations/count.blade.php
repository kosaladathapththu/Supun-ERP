@extends('layouts.app') @section('title',$count->document_number) @section('content')
<a href="{{ route('inventory-operations.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Inventory operations</a><h1 class="h3 page-title mt-2">{{ $count->document_number }}</h1><p class="text-muted">{{ $count->location->name }} Â· Enter the physically counted quantities.</p><form method="POST" action="{{ route('inventory-operations.count.post',$count) }}">@csrf<div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Product</th><th>System quantity</th><th class="pe-4">Counted quantity</th></tr></thead><tbody>@foreach($count->items as $i)<tr><td class="ps-4">{{ $i->product->name }}<div class="small text-muted">{{ $i->product->item_code }}</div></td><td>@quantity($i->system_quantity)</td><td class="pe-4"><input type="number" min="0" step="0.0001" name="items[{{ $i->id }}]" value="{{ $i->system_quantity }}" class="form-control" required></td></tr>@endforeach</tbody></table></div></div><button class="btn btn-success mt-3" onclick="return confirm('Post count variances to stock and accounting?')">Post count reconciliation</button></form>
@endsection
