@extends('layouts.app') @section('title','Start Stock Count') @section('content')
<a href="{{ route('inventory-operations.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Inventory operations</a><h1 class="h3 page-title mt-2">Start Physical Stock Count</h1><form method="POST" action="{{ route('inventory-operations.count.store') }}" class="card"><div class="card-body">@csrf<div class="mb-3"><label class="form-label">Location</label><select name="stock_location_id" class="form-select">@foreach($locations as $l)<option value="{{ $l->id }}">{{ $l->code }} — {{ $l->name }}</option>@endforeach</select></div><div class="mb-3"><label class="form-label">Notes</label><textarea name="notes" class="form-control"></textarea></div><button class="btn btn-primary">Create count sheet</button></div></form>
@endsection
