@extends('layouts.app') @section('title','Roles & Permissions') @section('content')
<div class="mb-4"><h1 class="h3 page-title">Roles & Permissions</h1><p class="text-muted">Module-level access profiles assigned to staff accounts.</p></div><div class="row g-4">@foreach($roles as $role)<div class="col-md-6 col-xl-4"><div class="card h-100"><div class="card-body"><div class="d-flex justify-content-between"><h2 class="h5">{{ $role->name }}</h2><span class="badge text-bg-light">{{ $role->users_count }} users</span></div><p class="text-muted">{{ $role->description }}</p><div class="small mb-3">{{ $role->permissions_count }} permissions</div>@if(in_array($role->slug,['main-admin','cfo'],true))<span class="badge text-bg-secondary">Protected full access</span>@else<a href="{{ route('admin.roles.edit',$role) }}" class="btn btn-sm btn-outline-primary">Edit permissions</a>@endif</div></div></div>@endforeach</div>
@endsection
