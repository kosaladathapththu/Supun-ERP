@extends('layouts.app') @section('title','Edit '.$role->name) @section('content')
<a href="{{ route('admin.roles.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Roles</a><h1 class="h3 page-title mt-2">Edit {{ $role->name }}</h1><form method="POST" action="{{ route('admin.roles.update',$role) }}">@csrf @method('PUT')<div class="card mb-4"><div class="card-body"><div class="row g-3"><div class="col-md-4"><label class="form-label">Role name</label><input name="name" value="{{ old('name',$role->name) }}" class="form-control" required></div><div class="col-md-8"><label class="form-label">Description</label><input name="description" value="{{ old('description',$role->description) }}" class="form-control"></div></div></div></div><div class="row g-3">@foreach($permissions as $module=>$items)<div class="col-md-6 col-xl-4"><div class="card h-100"><div class="card-body"><h2 class="h6 text-uppercase">{{ str($module)->headline() }}</h2>@foreach($items as $p)<label class="form-check"><input type="checkbox" name="permissions[]" value="{{ $p->id }}" class="form-check-input" {{ in_array($p->id,old('permissions',$role->permissions->pluck('id')->all()))?'checked':'' }}><span class="form-check-label">{{ str($p->slug)->after('.')->headline() }}</span></label>@endforeach</div></div></div>@endforeach</div><button class="btn btn-primary mt-4">Save permission matrix</button></form>
@endsection
