@extends('layouts.app') @section('title',$record->exists?'Edit Staff User':'New Staff User') @section('content')
<a href="{{ route('admin.users.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Staff users</a><h1 class="h3 page-title mt-2">{{ $record->exists?'Edit Staff User':'New Staff User' }}</h1><form method="POST" action="{{ $record->exists?route('admin.users.update',$record):route('admin.users.store') }}">@csrf @if($record->exists)@method('PUT')@endif<div class="card"><div class="card-body"><div class="row g-3"><div class="col-md-6"><label class="form-label">Name</label><input name="name" value="{{ old('name',$record->name) }}" class="form-control" required></div><div class="col-md-6"><label class="form-label">Email</label><input type="email" name="email" value="{{ old('email',$record->email) }}" class="form-control" required></div><div class="col-md-6"><label class="form-label">Phone</label><input name="phone" value="{{ old('phone',$record->phone) }}" class="form-control"></div><div class="col-md-6"><label class="form-label">{{ $record->exists?'New temporary password (optional)':'Temporary password' }}</label><input type="password" name="password" class="form-control" {{ $record->exists?'':'required' }} autocomplete="new-password"><div class="form-text">At least 12 characters with uppercase, lowercase, number and symbol.</div></div><div class="col-md-6"><label class="form-label">Confirm password</label><input type="password" name="password_confirmation" class="form-control" {{ $record->exists?'':'required' }}></div><div class="col-md-6"><label class="form-label d-block">Roles</label>@foreach($roles as $role)<label class="form-check form-check-inline"><input type="checkbox" class="form-check-input" name="roles[]" value="{{ $role->id }}" {{ in_array($role->id,old('roles',$record->exists?$record->roles->pluck('id')->all():[]))?'checked':'' }}><span class="form-check-label">{{ $role->name }}</span></label>@endforeach</div><div class="col-12"><label class="form-check"><input type="checkbox" name="is_active" value="1" class="form-check-input" {{ old('is_active',$record->exists?$record->is_active:true)?'checked':'' }}><span class="form-check-label">Active account</span></label></div></div><button class="btn btn-primary mt-4">{{ $record->exists?'Save changes':'Create staff user' }}</button></div></div></form>
@endsection
