@extends('layouts.app') @section('title','Staff Users') @section('content')
<div class="d-flex justify-content-between mb-4"><div><h1 class="h3 page-title">Staff Users</h1><p class="text-muted">Individual accounts, access roles and login status.</p></div><a href="{{ route('admin.users.create') }}" class="btn btn-primary"><i class="bi bi-person-plus"></i> New staff user</a></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">User</th><th>Phone</th><th>Roles</th><th>Last login</th><th>Status</th><th class="pe-4"></th></tr></thead><tbody>@foreach($users as $u)<tr><td class="ps-4"><strong>{{ $u->name }}</strong><div class="small text-muted">{{ $u->email }}</div></td><td>{{ $u->phone ?: '—' }}</td><td>@foreach($u->roles as $role)<span class="badge text-bg-light me-1">{{ $role->name }}</span>@endforeach</td><td>{{ $u->last_login_at?->format('Y-m-d H:i') ?: 'Never' }}</td><td><span class="badge {{ $u->is_active?'text-bg-success':'text-bg-secondary' }}">{{ $u->is_active?'Active':'Inactive' }}</span>@if(!$u->password_changed_at)<div class="small text-warning">Password change required</div>@endif</td><td class="pe-4"><a href="{{ route('admin.users.edit',$u) }}" class="btn btn-sm btn-outline-primary">Edit</a></td></tr>@endforeach</tbody></table></div><div class="p-3">{{ $users->links() }}</div></div>
@endsection
