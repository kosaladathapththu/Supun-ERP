@extends('layouts.app')
@section('title',Str::plural($title))
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 page-title mb-1">{{ Str::plural($title) }}</h1><p class="text-muted mb-0">Manage {{ strtolower(Str::plural($title)) }} used throughout the ERP.</p></div><a href="{{ route($route.'.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i> Add {{ $title }}</a></div>
<div class="card"><div class="card-body p-0"><div class="p-3 border-bottom"><form class="row g-2"><div class="col-md-5"><div class="input-group"><span class="input-group-text bg-white"><i class="bi bi-search"></i></span><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search code or name"></div></div><div class="col-auto"><button class="btn btn-outline-secondary">Search</button></div>@if(request('search'))<div class="col-auto"><a class="btn btn-light" href="{{ route($route.'.index') }}">Clear</a></div>@endif</form></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th class="ps-4">Code</th><th>Name</th><th>Status</th><th class="text-end pe-4">Actions</th></tr></thead><tbody>@forelse($records as $record)<tr><td class="ps-4 fw-semibold">{{ $record->code }}</td><td>{{ $record->name }}</td><td><span class="badge {{ $record->is_active?'text-bg-success':'text-bg-secondary' }}">{{ $record->is_active?'Active':'Inactive' }}</span></td><td class="text-end pe-4"><a class="btn btn-sm btn-light" href="{{ route($route.'.edit',$record) }}"><i class="bi bi-pencil"></i> Edit</a></td></tr>@empty<tr><td colspan="4" class="text-center py-5 text-muted"><i class="bi bi-inbox d-block fs-2 mb-2"></i>No records found.</td></tr>@endforelse</tbody></table></div>@if($records->hasPages())<div class="p-3 border-top">{{ $records->links() }}</div>@endif</div></div>
@endsection
