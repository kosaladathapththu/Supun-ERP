@extends('layouts.app') @section('title',$order->document_number) @section('content')
<div class="d-flex justify-content-between mb-4"><div><a href="{{ route('sales-orders.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Sales orders</a><h1 class="h3 page-title mt-2">{{ $order->document_number }}</h1><p class="text-muted">{{ $order->customer->name }} · Source {{ $order->quotation?->document_number ?: 'Direct' }}</p></div>@if(in_array($order->status,['confirmed','partially_delivered']))<a href="{{ route('sales-orders.deliver',$order) }}" class="btn btn-success align-self-start"><i class="bi bi-truck"></i> Create delivery</a>@endif</div><div class="card mb-4"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Product</th><th class="text-end">Ordered</th><th class="text-end">Delivered</th><th class="text-end">Remaining</th><th class="text-end pe-4">Unit price</th></tr></thead><tbody>@foreach($order->items as $i)<tr><td class="ps-4">{{ $i->product->name }}</td><td class="text-end">{{ number_format($i->quantity,4) }}</td><td class="text-end">{{ number_format($i->delivered_quantity,4) }}</td><td class="text-end">{{ number_format($i->quantity-$i->delivered_quantity,4) }}</td><td class="text-end pe-4">{{ number_format($i->unit_price,2) }}</td></tr>@endforeach</tbody></table></div></div><h2 class="h5">Deliveries and invoices</h2><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Delivery note</th><th>Date</th><th>Invoice</th><th>Status</th></tr></thead><tbody>@forelse($order->deliveries as $d)<tr><td class="ps-4"><a href="{{ route('delivery-notes.show',$d) }}">{{ $d->document_number }}</a></td><td>{{ $d->delivery_date->format('Y-m-d H:i') }}</td><td><a href="{{ route('sales.show',$d->sale) }}">{{ $d->sale->document_number }}</a></td><td>{{ str($d->status)->headline() }}</td></tr>@empty<tr><td colspan="4" class="text-center text-muted py-4">Nothing delivered yet.</td></tr>@endforelse</tbody></table></div></div>
@endsection
