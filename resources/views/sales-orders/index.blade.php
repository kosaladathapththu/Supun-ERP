@extends('layouts.app') @section('title','Sales Orders') @section('content')
<div class="mb-4"><h1 class="h3 page-title">Sales Orders</h1><p class="text-muted">Confirmed customer orders with partial-delivery tracking.</p></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Order</th><th>Quotation</th><th>Customer</th><th>Date</th><th>Total</th><th>Status</th></tr></thead><tbody>@forelse($orders as $o)<tr><td class="ps-4"><a href="{{ route('sales-orders.show',$o) }}">{{ $o->document_number }}</a></td><td>{{ $o->quotation?->document_number ?: 'Direct' }}</td><td>{{ $o->customer->name }}</td><td>{{ $o->order_date->format('Y-m-d') }}</td><td>Rs. {{ number_format($o->total_amount,2) }}</td><td><span class="badge text-bg-{{ $o->status==='completed'?'success':'primary' }}">{{ str($o->status)->headline() }}</span></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No sales orders. Convert a quotation first.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $orders->links() }}</div></div>
@endsection
