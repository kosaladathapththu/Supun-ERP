@extends('layouts.app') @section('title',Str::plural($title)) @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 page-title mb-1">{{ Str::plural($title) }}</h1><p class="text-muted mb-0">Search and maintain {{ strtolower(Str::plural($title)) }}.</p></div><a class="btn btn-primary" href="{{ route($route.'.create') }}"><i class="bi bi-plus-lg"></i> Add {{ $title }}</a></div><div class="card"><div class="p-3 border-bottom"><form class="d-flex gap-2"><input class="form-control" style="max-width:420px" name="search" value="{{ request('search') }}" placeholder="Search code, name or phone"><button class="btn btn-outline-secondary">Search</button></form></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th class="ps-4">Code</th><th>Name</th><th>Phone</th><th>Status</th><th class="text-end pe-4">Action</th></tr></thead><tbody>@forelse($records as $record)<tr><td class="ps-4 fw-semibold">{{ $record->code }}</td><td>{{ $record->name }}</td><td>{{ $record->phone?:'—' }}</td><td><span class="badge {{ $record->is_active?'text-bg-success':'text-bg-secondary' }}">{{ $record->is_active?'Active':'Inactive' }}</span></td><td class="text-end pe-4"><a class="btn btn-light btn-sm" href="{{ route($route.'.edit',$record) }}"><i class="bi bi-pencil"></i> Edit</a></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No records found.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $records->links() }}</div></div>
@endsection
