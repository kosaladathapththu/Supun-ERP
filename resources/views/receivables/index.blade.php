@extends('layouts.app') @section('title','Customer Receipts') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 page-title mb-1">Customer Receipts</h1><p class="text-muted mb-0">Allocate payments to invoices and track advances.</p></div><div class="d-flex gap-2"><a href="{{ route('receivables.aging') }}" class="btn btn-outline-primary">Aging Report</a><a href="{{ route('receivables.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg"></i> New Receipt</a></div></div>
<div class="card"><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-5"><select name="customer_id" class="form-select"><option value="">All customers</option>@foreach($customers as $c)<option value="{{ $c->id }}" @selected(request('customer_id')==$c->id)>{{ $c->name }}</option>@endforeach</select></div><div class="col"><button class="btn btn-outline-secondary">Filter</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Receipt</th><th>Date</th><th>Customer</th><th>Method</th><th class="text-end">Amount</th><th class="text-end">Unapplied</th></tr></thead><tbody>@forelse($receipts as $x)<tr><td><a href="{{ route('receivables.show',$x) }}">{{ $x->receipt_number }}</a></td><td>{{ $x->receipt_date->format('d M Y') }}</td><td>{{ $x->customer->name }}</td><td>{{ str($x->payment_method)->headline() }}</td><td class="text-end">{{ number_format($x->amount,2) }}</td><td class="text-end">{{ number_format($x->unapplied_amount,2) }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No customer receipts recorded.</td></tr>@endforelse</tbody></table></div>{{ $receipts->links() }}</div></div>
@endsection
