@extends('layouts.app') @section('title','Customer Ledger') @section('content')
<div class="d-flex justify-content-between mb-4"><div><h1 class="h3 page-title">{{ $customer->name }}</h1><p class="text-muted mb-0">Receivables ledger · {{ $customer->code }}</p></div><a href="{{ route('receivables.create',['customer_id'=>$customer->id]) }}" class="btn btn-primary">Receive Payment</a></div><div class="card"><div class="card-body"><table class="table"><thead><tr><th>Date</th><th>Document</th><th>Type</th><th class="text-end">Debit</th><th class="text-end">Credit</th></tr></thead><tbody>@if((float)$customer->opening_balance!=0)<tr><td>{{ optional($customer->opening_balance_date)->format('d M Y') }}</td><td>Opening balance</td><td>Opening</td><td class="text-end">{{ number_format($customer->opening_balance,2) }}</td><td></td></tr>@endif @foreach($sales as $s)<tr><td>{{ $s->sale_date->format('d M Y') }}</td><td><a href="{{ route('sales.show',$s) }}">{{ $s->document_number }}</a></td><td>Invoice</td><td class="text-end">{{ number_format($s->grand_total,2) }}</td><td></td></tr>@endforeach @foreach($receipts as $x)<tr><td>{{ $x->receipt_date->format('d M Y') }}</td><td><a href="{{ route('receivables.show',$x) }}">{{ $x->receipt_number }}</a></td><td>Receipt</td><td></td><td class="text-end">{{ number_format($x->amount,2) }}</td></tr>@endforeach</tbody></table></div></div>
@endsection
