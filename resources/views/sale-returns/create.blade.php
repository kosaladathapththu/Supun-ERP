@extends('layouts.app') @section('title','Return '.$sale->document_number) @section('content')<div class="mb-4"><a href="{{ route('sales.show',$sale) }}">← {{ $sale->document_number }}</a><h1 class="h3 mt-2">Create Sales Return</h1><p>{{ $sale->customer->name }}</p></div><form method="POST" action="{{ route('sale-returns.store',$sale) }}">@csrf<div class="card mb-4"><table class="table mb-0"><thead><tr><th class="ps-4">Product</th><th>Sold</th><th>Returned</th><th>Return now</th><th>Condition</th></tr></thead><tbody>@foreach($sale->items as $i)@php($previous=$returned[$i->id]??0)<tr><td class="ps-4">{{ $i->product->name }}</td><td>{{ $i->quantity }}</td><td>{{ $previous }}</td><td><input class="form-control" type="number" step=".0001" min="0" max="{{ $i->quantity-$previous }}" name="items[{{ $i->id }}][quantity]" value="0"></td><td><select class="form-select" name="items[{{ $i->id }}][condition]"><option value="resalable">Resalable</option><option value="damaged">Damaged</option></select></td></tr>@endforeach</tbody></table></div><div class="card"><div class="card-body row g-3"><div class="col-md-4"><label>Settlement</label><select class="form-select" name="settlement_type"><option value="credit_note">Credit note / reduce outstanding</option><option value="cash_refund">Cash refund</option></select></div><div class="col-md-8"><label>Reason</label><input class="form-control" name="reason" required></div><div><button class="btn btn-danger">Post Sales Return</button></div></div></div></form>@endsection
