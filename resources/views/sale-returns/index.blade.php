@extends('layouts.app') @section('title','Sales Returns') @section('content')<div class="mb-4"><h1 class="h3 page-title">Sales Returns</h1><p class="text-muted">Open an invoice and use Return to create a full or partial return.</p></div><div class="card"><table class="table mb-0"><thead><tr><th class="ps-4">Return</th><th>Invoice</th><th>Customer</th><th>Date</th><th>Settlement</th><th>Total</th></tr></thead><tbody>@forelse($returns as $x)<tr><td class="ps-4"><a href="{{ route('sale-returns.show',$x) }}">{{ $x->document_number }}</a></td><td>{{ $x->sale->document_number }}</td><td>{{ $x->customer->name }}</td><td>{{ $x->return_date->format('Y-m-d H:i') }}</td><td>{{ Str::headline($x->settlement_type) }}</td><td>Rs. {{ number_format($x->total_amount,2) }}</td></tr>@empty<tr><td colspan="6" class="text-center py-5">No sales returns yet.</td></tr>@endforelse</tbody></table><div class="p-3">{{ $returns->links() }}</div></div>@endsection
