@extends('layouts.app') @section('title','Sales Returns') @section('content')
<div class="mb-4"><h1 class="h3 page-title">Sales Returns</h1><p class="text-muted">Select an invoice for a full or partial customer return.</p></div><div class="card mb-4"><div class="card-body"><h2 class="h5">Choose a sales invoice</h2><div class="row g-2 mt-1">@forelse($sales as $sale)<div class="col-md-6 col-xl-4"><a href="{{ route('sale-returns.create',$sale) }}" class="btn btn-outline-primary w-100 text-start"><strong>{{ $sale->document_number }}</strong><br><small>{{ $sale->customer->name }} · Rs. {{ number_format($sale->grand_total,2) }}</small></a></div>@empty<div class="text-muted">No posted sales invoices are available.</div>@endforelse</div></div></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Return</th><th>Invoice</th><th>Customer</th><th>Date</th><th>Settlement</th><th>Total</th></tr></thead><tbody>@forelse($returns as $x)<tr><td class="ps-4"><a href="{{ route('sale-returns.show',$x) }}">{{ $x->document_number }}</a></td><td><a href="{{ route('sales.show',$x->sale) }}">{{ $x->sale->document_number }}</a></td><td>{{ $x->customer->name }}</td><td>{{ $x->return_date->format('Y-m-d H:i') }}</td><td>{{ Str::headline($x->settlement_type) }}</td><td>Rs. {{ number_format($x->total_amount,2) }}</td></tr>@empty<tr><td colspan="6" class="text-center py-5">No sales returns yet.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $returns->links() }}</div></div>
@endsection
