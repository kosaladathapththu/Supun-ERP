@extends('layouts.app') @section('title',$return->document_number) @section('content')<h1 class="h3">{{ $return->document_number }}</h1><p>{{ $return->customer->name }} · {{ $return->sale->document_number }}</p><div class="card"><table class="table mb-0"><thead><tr><th class="ps-4">Product</th><th>Quantity</th><th>Condition</th><th>Total</th></tr></thead><tbody>@foreach($return->items as $i)<tr><td class="ps-4">{{ $i->product->name }}</td><td>{{ $i->quantity }}</td><td>{{ Str::headline($i->condition) }}</td><td>Rs. {{ number_format($i->line_total,2) }}</td></tr>@endforeach</tbody><tfoot><tr><th colspan="3" class="text-end">Total</th><th>Rs. {{ number_format($return->total_amount,2) }}</th></tr></tfoot></table></div>@if($return->creditNote)<div class="alert alert-info mt-3">Credit note {{ $return->creditNote->document_number }}: Rs. {{ number_format($return->creditNote->amount,2) }}</div>@endif @endsection
