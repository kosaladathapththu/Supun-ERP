@extends('layouts.app') @section('title','Product Profitability') @section('content')
<div class="d-flex justify-content-between flex-wrap gap-3 mb-4"><div><a href="{{ route('reports.index',['from'=>$from,'to'=>$to]) }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Management Intelligence</a><h1 class="h3 page-title mt-2">Product Profitability</h1><p class="text-muted">Net of posted returns · {{ $from }} to {{ $to }}</p></div><a href="{{ route('reports.profitability.export',['from'=>$from,'to'=>$to]) }}" class="btn btn-outline-success align-self-start"><i class="bi bi-file-earmark-spreadsheet"></i> Export CSV</a></div><form class="card card-body mb-4"><div class="row g-2"><div class="col-md-3"><input type="date" name="from" value="{{ $from }}" class="form-control"></div><div class="col-md-3"><input type="date" name="to" value="{{ $to }}" class="form-control"></div><div class="col"><button class="btn btn-primary">Apply</button></div></div></form><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Product</th><th>Category / Brand</th><th class="text-end">Net Qty</th><th class="text-end">Net Sales</th><th class="text-end">Cost</th><th class="text-end">Gross Profit</th><th class="text-end pe-4">Margin</th></tr></thead><tbody>@forelse($rows as $x)<tr><td class="ps-4"><strong>{{ $x->product->name }}</strong><div class="small text-muted">{{ $x->product->item_code }}</div></td><td>{{ $x->product->category?->name ?: '—' }}<div class="small text-muted">{{ $x->product->brand?->name ?: 'No brand' }}</div></td><td class="text-end">{{ number_format($x->quantity,2) }}</td><td class="text-end">{{ number_format($x->sales,2) }}</td><td class="text-end">{{ number_format($x->cost,2) }}</td><td class="text-end {{ $x->profit<0?'text-danger':'text-success' }}">{{ number_format($x->profit,2) }}</td><td class="text-end pe-4">{{ number_format($x->margin,1) }}%</td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No sales activity in this period.</td></tr>@endforelse</tbody></table></div></div>
@endsection
