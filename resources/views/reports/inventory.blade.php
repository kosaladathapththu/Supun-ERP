@extends('layouts.app') @section('title','Inventory Valuation') @section('content')
<div class="d-flex justify-content-between flex-wrap gap-3 mb-4"><div><a href="{{ route('reports.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Management Intelligence</a><h1 class="h3 page-title mt-2">Inventory Valuation</h1><p class="text-muted">Current quantity valued at weighted-average cost.</p></div><a href="{{ route('reports.inventory.export') }}" class="btn btn-outline-success align-self-start"><i class="bi bi-file-earmark-spreadsheet"></i> Export CSV</a></div><div class="row g-3 mb-4">@foreach([['Total value',$rows->sum('value')],['Healthy items',$rows->where('status','healthy')->count()],['Low-stock items',$rows->where('status','low_stock')->count()],['Out-of-stock items',$rows->where('status','out_of_stock')->count()]] as [$label,$value])<div class="col-md-3"><div class="card"><div class="card-body"><div class="small text-muted">{{ $label }}</div><div class="h4 mb-0">{{ $label==='Total value'?'Rs. '.number_format($value,2):number_format($value) }}</div></div></div></div>@endforeach</div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Product</th><th>Category / Brand</th><th class="text-end">Quantity</th><th class="text-end">Average Cost</th><th class="text-end">Stock Value</th><th class="pe-4">Status</th></tr></thead><tbody>@foreach($rows as $x)<tr><td class="ps-4"><a href="{{ route('stock.ledger',$x->product) }}" class="fw-semibold">{{ $x->product->name }}</a><div class="small text-muted">{{ $x->product->item_code }}</div></td><td>{{ $x->product->category?->name ?: '—' }}<div class="small text-muted">{{ $x->product->brand?->name ?: 'No brand' }}</div></td><td class="text-end">{{ number_format($x->quantity,4) }}</td><td class="text-end">{{ number_format($x->average_cost,4) }}</td><td class="text-end">{{ number_format($x->value,2) }}</td><td class="pe-4"><span class="badge {{ $x->status==='healthy'?'text-bg-success':($x->status==='low_stock'?'text-bg-warning':'text-bg-danger') }}">{{ str($x->status)->headline() }}</span></td></tr>@endforeach</tbody><tfoot><tr><th colspan="4" class="text-end">Total</th><th class="text-end">{{ number_format($rows->sum('value'),2) }}</th><th></th></tr></tfoot></table></div></div>
@endsection
