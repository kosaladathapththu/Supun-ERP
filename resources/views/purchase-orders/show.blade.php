@extends('layouts.app') @section('title',$order->document_number) @section('content')
<div class="d-flex justify-content-between mb-4"><div><a href="{{ route('purchase-orders.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Purchase orders</a><h1 class="h3 page-title mt-2">{{ $order->document_number }}</h1><p class="text-muted">{{ $order->supplier->name }} · {{ $order->order_date->format('Y-m-d') }}</p></div><div>@if($order->status==='draft')<form method="POST" action="{{ route('purchase-orders.confirm',$order) }}">@csrf<button class="btn btn-primary">Confirm Order</button></form>@elseif(in_array($order->status,['confirmed','partially_received']))<a class="btn btn-success" href="{{ route('grn.create',$order) }}"><i class="bi bi-box-arrow-in-down"></i> Receive GRN</a>@endif</div></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Product</th><th>Ordered</th><th>Received</th><th>Remaining</th><th>Unit cost</th><th class="pe-4">Total</th></tr></thead><tbody>@foreach($order->items as $i)<tr><td class="ps-4"><strong>{{ $i->product->name }}</strong><div class="small text-muted">{{ $i->product->item_code }}</div></td><td>{{ number_format($i->quantity,2) }}</td><td>{{ number_format($i->received_quantity,2) }}</td><td>{{ number_format($i->quantity-$i->received_quantity,2) }}</td><td>Rs. {{ number_format($i->unit_cost,2) }}</td><td class="pe-4">Rs. {{ number_format($i->line_total,2) }}</td></tr>@endforeach</tbody><tfoot><tr><th colspan="5" class="text-end">Order Total</th><th>Rs. {{ number_format($order->total_amount,2) }}</th></tr></tfoot></table></div></div>
@endsection
