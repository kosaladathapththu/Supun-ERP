@extends('layouts.app') @section('title','Stock Ledger') @section('content')
<div class="mb-4"><a href="{{ route('stock.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Current stock</a><h1 class="h3 page-title mt-2">{{ $product->name }}</h1><p class="text-muted">Stock ledger  -  {{ $product->item_code }}  -  Current @quantity($product->current_quantity)</p></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Date</th><th>Reference</th><th>Movement</th><th>Qty In</th><th>Qty Out</th><th>Balance</th><th>Unit Cost</th><th>Value</th></tr></thead><tbody>@forelse($movements as $m)<tr><td class="ps-4">{{ $m->movement_at }}</td><td>{{ $m->reference_number }}</td><td>{{ Str::headline($m->movement_type) }}</td><td class="text-success">@quantity($m->quantity_in)</td><td class="text-danger">@quantity($m->quantity_out)</td><td class="fw-semibold">@quantity($m->balance_quantity)</td><td>Rs. {{ number_format($m->unit_cost,4) }}</td><td>Rs. {{ number_format($m->stock_value,2) }}</td></tr>@empty<tr><td colspan="8" class="text-center py-5 text-muted">No stock movements yet.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $movements->links() }}</div></div>
@endsection
