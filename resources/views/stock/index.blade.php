@extends('layouts.app') @section('title','Current Stock') @section('content')
<div class="d-flex justify-content-between mb-4"><div><h1 class="h3 page-title">Current Stock</h1><p class="text-muted">Cached quantities reconciled from immutable stock movements.</p></div><div class="card"><div class="card-body py-2 px-4"><small class="text-muted">Total cost value</small><div class="fw-bold">Rs. {{ number_format($costValue,2) }}</div></div></div></div><div class="card"><div class="p-3 border-bottom"><form class="d-flex gap-2"><input class="form-control" style="max-width:420px" name="search" value="{{ request('search') }}" placeholder="Search item code or product"><button class="btn btn-outline-secondary">Search</button></form></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Product</th><th>Category</th><th>On Hand</th><th>Average Cost</th><th>Stock Value</th><th>Reorder</th><th></th></tr></thead><tbody>@forelse($products as $p)<tr><td class="ps-4"><strong>{{ $p->name }}</strong><div class="small text-muted">{{ $p->item_code }}</div></td><td>{{ $p->category->name }}</td><td class="fw-semibold">{{ number_format($p->current_quantity,4) }} {{ $p->unit->code }}</td><td>Rs. {{ number_format($p->average_cost,4) }}</td><td>Rs. {{ number_format($p->current_quantity*$p->average_cost,2) }}</td><td>@if($p->current_quantity<=$p->reorder_level)<span class="badge text-bg-warning">Low stock</span>@else<span class="badge text-bg-success">OK</span>@endif</td><td class="pe-4"><a class="btn btn-light btn-sm" href="{{ route('stock.ledger',$p) }}">Ledger</a></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">No products available.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $products->links() }}</div></div>
@endsection
