@extends('layouts.app') @section('title','General Ledger') @section('content')
<div class="mb-4"><a href="{{ route('accounting.trial-balance') }}" class="text-decoration-none">← Trial Balance</a><h1 class="h3 page-title mt-2">{{ $account->code }} — {{ $account->name }}</h1></div><div class="card"><div class="card-body"><table class="table"><thead><tr><th>Date</th><th>Journal</th><th>Reference</th><th class="text-end">Debit</th><th class="text-end">Credit</th><th class="text-end">Balance</th></tr></thead><tbody>@php($balance=0) @forelse($lines as $line) @php($balance+=(float)$line->debit-(float)$line->credit)<tr><td>{{ $line->entry->entry_date->format('d M Y') }}</td><td><a href="{{ route('accounting.show',$line->entry) }}">{{ $line->entry->journal_number }}</a></td><td>{{ $line->entry->reference_number }}</td><td class="text-end">{{ $line->debit>0?number_format($line->debit,2):'' }}</td><td class="text-end">{{ $line->credit>0?number_format($line->credit,2):'' }}</td><td class="text-end fw-semibold">{{ number_format($balance,2) }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No ledger activity.</td></tr>@endforelse</tbody></table></div></div>
@endsection
