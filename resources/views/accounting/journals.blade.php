@extends('layouts.app') @section('title','Accounting Journals') @section('content')
<div class="d-flex justify-content-between mb-4"><div><h1 class="h3 page-title">Accounting Journals</h1><p class="text-muted">Immutable double-entry postings from operational transactions.</p></div><a href="{{ route('accounting.trial-balance') }}" class="btn btn-outline-primary align-self-start">Trial Balance</a></div><div class="card"><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-3"><input type="date" name="from" value="{{ request('from') }}" class="form-control"></div><div class="col-md-3"><input type="date" name="to" value="{{ request('to') }}" class="form-control"></div><div class="col"><button class="btn btn-outline-secondary">Filter</button></div></form><table class="table"><thead><tr><th>Journal</th><th>Date</th><th>Reference</th><th>Description</th><th>Status</th></tr></thead><tbody>@forelse($entries as $e)<tr><td><a href="{{ route('accounting.show',$e) }}">{{ $e->journal_number }}</a></td><td>{{ $e->entry_date->format('d M Y') }}</td><td>{{ $e->reference_number }}</td><td>{{ $e->description }}</td><td><span class="badge text-bg-success">{{ ucfirst($e->status) }}</span></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No journals posted.</td></tr>@endforelse</tbody></table>{{ $entries->links() }}</div></div>
@endsection
