@extends('layouts.app') @section('title',$journal->journal_number) @section('content')
<div class="mb-4"><a href="{{ route('accounting.journals') }}" class="text-decoration-none">← Journals</a><h1 class="h3 page-title mt-2">{{ $journal->journal_number }}</h1><p class="text-muted">{{ $journal->description }} · {{ $journal->entry_date->format('d M Y') }}</p></div><div class="card"><div class="card-body"><table class="table"><thead><tr><th>Account</th><th>Description</th><th class="text-end">Debit</th><th class="text-end">Credit</th></tr></thead><tbody>@foreach($journal->lines as $line)<tr><td><a href="{{ route('accounting.ledger',$line->account) }}">{{ $line->account->code }} — {{ $line->account->name }}</a></td><td>{{ $line->description }}</td><td class="text-end">{{ $line->debit>0?number_format($line->debit,2):'' }}</td><td class="text-end">{{ $line->credit>0?number_format($line->credit,2):'' }}</td></tr>@endforeach</tbody><tfoot><tr class="fw-bold"><td colspan="2">Total</td><td class="text-end">{{ number_format($journal->lines->sum('debit'),2) }}</td><td class="text-end">{{ number_format($journal->lines->sum('credit'),2) }}</td></tr></tfoot></table></div></div>
@endsection
