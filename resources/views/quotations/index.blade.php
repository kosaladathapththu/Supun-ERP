@extends('layouts.app') @section('title','Quotations') @section('content')
<div class="d-flex justify-content-between mb-4"><div><h1 class="h3 page-title">Quotations</h1><p class="text-muted">Customer offers that can be converted into traceable sales orders.</p></div><a href="{{ route('quotations.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg"></i> New quotation</a></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Quotation</th><th>Customer</th><th>Date</th><th>Valid until</th><th>Total</th><th>Status</th></tr></thead><tbody>@forelse($quotations as $q)<tr><td class="ps-4"><a href="{{ route('quotations.show',$q) }}">{{ $q->document_number }}</a></td><td>{{ $q->customer->name }}</td><td>{{ $q->quotation_date->format('Y-m-d') }}</td><td>{{ $q->valid_until?->format('Y-m-d') ?: '—' }}</td><td>Rs. {{ number_format($q->total_amount,2) }}</td><td><span class="badge text-bg-{{ $q->status==='converted'?'success':'primary' }}">{{ str($q->status)->headline() }}</span></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No quotations yet.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $quotations->links() }}</div></div>
@endsection
