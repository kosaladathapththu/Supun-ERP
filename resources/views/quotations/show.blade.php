@extends('layouts.app') @section('title',$quotation->document_number) @section('content')
<div class="d-flex justify-content-between mb-4"><div><a href="{{ route('quotations.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Quotations</a><h1 class="h3 page-title mt-2">{{ $quotation->document_number }}</h1><p class="text-muted">{{ $quotation->customer->name }} · {{ $quotation->quotation_date->format('Y-m-d') }}</p></div><div>@if($quotation->salesOrder)<a href="{{ route('sales-orders.show',$quotation->salesOrder) }}" class="btn btn-outline-primary">Open {{ $quotation->salesOrder->document_number }}</a>@elseif(in_array($quotation->status,['draft','sent','accepted']))<form method="POST" action="{{ route('quotations.convert',$quotation) }}">@csrf<button class="btn btn-success" onclick="return confirm('Convert this quotation to a confirmed sales order?')">Convert to sales order</button></form>@endif</div></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Product</th><th class="text-end">Quantity</th><th class="text-end">Unit price</th><th class="text-end pe-4">Line total</th></tr></thead><tbody>@foreach($quotation->items as $i)<tr><td class="ps-4">{{ $i->product->name }}</td><td class="text-end">{{ number_format($i->quantity,4) }}</td><td class="text-end">{{ number_format($i->unit_price,2) }}</td><td class="text-end pe-4">{{ number_format($i->line_total,2) }}</td></tr>@endforeach</tbody><tfoot><tr><th colspan="3" class="text-end">Subtotal</th><th class="text-end pe-4">{{ number_format($quotation->subtotal,2) }}</th></tr><tr><th colspan="3" class="text-end">Discount</th><th class="text-end pe-4">{{ number_format($quotation->discount_amount,2) }}</th></tr><tr><th colspan="3" class="text-end">Total</th><th class="text-end pe-4">{{ number_format($quotation->total_amount,2) }}</th></tr></tfoot></table></div></div>
@endsection
