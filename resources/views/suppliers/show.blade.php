@extends('layouts.app')
@section('title',$supplier->name)
@section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><a href="{{ route('suppliers.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Suppliers</a><h1 class="h3 page-title mt-2 mb-1">{{ $supplier->name }}</h1><p class="text-muted mb-0">{{ $supplier->code }} · {{ $supplier->is_active?'Active supplier':'Inactive supplier' }}</p></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" href="{{ route('payables.ledger',$supplier) }}"><i class="bi bi-journal-text me-1"></i>Ledger</a><a class="btn btn-primary" href="{{ route('suppliers.edit',$supplier) }}"><i class="bi bi-pencil me-1"></i>Edit Supplier</a></div></div>
<div class="row g-3 mb-4"><div class="col-md-3"><div class="card h-100"><div class="card-body"><div class="text-muted small">Opening balance</div><div class="h4 mb-0">Rs. {{ number_format((float)$supplier->opening_balance,2) }}</div><small class="text-muted">{{ optional($supplier->opening_balance_date)->format('d M Y')?:'No opening date' }}</small></div></div></div><div class="col-md-3"><div class="card h-100"><div class="card-body"><div class="text-muted small">Supplier bills</div><div class="h4 mb-0">Rs. {{ number_format($totalBilled,2) }}</div></div></div></div><div class="col-md-3"><div class="card h-100"><div class="card-body"><div class="text-muted small">Payments and credits</div><div class="h4 mb-0 text-success">Rs. {{ number_format($totalPaid+$totalCredits,2) }}</div></div></div></div><div class="col-md-3"><div class="card h-100 border-start border-danger border-4"><div class="card-body"><div class="text-muted small">Current payable</div><div class="h4 mb-0 text-danger">Rs. {{ number_format($currentPayable,2) }}</div></div></div></div></div>
<div class="row g-3"><div class="col-lg-4"><div class="card h-100"><div class="card-header bg-white p-3 fw-semibold">Supplier information</div><div class="card-body"><dl class="row mb-0"><dt class="col-5">Contact</dt><dd class="col-7">{{ $supplier->contact_person?:'—' }}</dd><dt class="col-5">Phone</dt><dd class="col-7">{{ $supplier->phone?:'—' }}</dd><dt class="col-5">Email</dt><dd class="col-7">{{ $supplier->email?:'—' }}</dd><dt class="col-5">Address</dt><dd class="col-7">{{ $supplier->address?:'—' }}</dd></dl></div></div></div><div class="col-lg-8"><div class="card"><div class="card-header bg-white p-3 d-flex justify-content-between"><strong>Recent supplier invoices</strong><a href="{{ route('payables.create',['supplier_id'=>$supplier->id]) }}">Pay supplier</a></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-3">Invoice</th><th>Date</th><th class="text-end">Total</th><th class="text-end pe-3">Balance</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td class="ps-3"><strong>{{ $invoice->document_number }}</strong><div class="small text-muted">{{ $invoice->supplier_invoice_number?:'No supplier reference' }}</div></td><td>{{ $invoice->invoice_date->format('d M Y') }}</td><td class="text-end">{{ number_format($invoice->total_amount,2) }}</td><td class="text-end pe-3 fw-semibold">{{ number_format($invoice->balance_amount,2) }}</td></tr>@empty<tr><td colspan="4" class="text-center text-muted py-4">No supplier invoices yet.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
