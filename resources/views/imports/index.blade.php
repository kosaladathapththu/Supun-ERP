@extends('layouts.app')
@section('title','Bulk Inventory Import')
@section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><h1 class="h3 page-title mb-1">Bulk Product & Inventory Import</h1><p class="text-muted mb-0">Add products, opening stock, or replenish existing inventory in bulk.</p></div><a class="btn btn-outline-primary" href="{{ route('imports.template') }}"><i class="bi bi-download"></i> Download Import Template</a></div>
<div class="alert alert-info"><strong>How quantity works:</strong> for a new product, quantity becomes opening stock. For an existing item code, quantity adds new stock and creates the linked purchase, GRN, supplier invoice and payable.</div>
<div class="row g-4"><div class="col-lg-5"><div class="card"><div class="card-body p-4"><h2 class="h5 fw-bold">Upload Excel or CSV</h2><p class="text-muted small">Excel `.xlsx` and CSV are supported. Keep item codes, phones and barcodes formatted as Text. Maximum 5,000 rows and 10 MB.</p><form method="POST" action="{{ route('imports.store') }}" enctype="multipart/form-data">@csrf<label class="form-label">Completed inventory template</label><input class="form-control mb-3" type="file" name="file" accept=".xlsx,.csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,text/csv" required><button class="btn btn-primary"><i class="bi bi-cloud-arrow-up me-1"></i> Validate and Preview</button></form><div class="alert alert-light border mt-4 mb-0 small"><i class="bi bi-info-circle me-1"></i>Supplier code is optional. The system matches an existing supplier by phone or name, or creates a new supplier with the next SUP-### code.</div></div></div></div>
<div class="col-lg-7"><div class="card"><div class="card-body p-0"><div class="p-4 border-bottom"><h2 class="h5 fw-bold mb-0">Import history</h2></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">File</th><th>Rows</th><th>Status</th><th>Date</th></tr></thead><tbody>@forelse($batches as $batch)<tr><td class="ps-4"><a href="{{ route('imports.show',$batch) }}" class="fw-semibold text-decoration-none">{{ $batch->original_filename }}</a></td><td>{{ $batch->valid_rows }}/{{ $batch->total_rows }} valid</td><td><span class="badge {{ $batch->status==='imported'?'text-bg-success':'text-bg-warning' }}">{{ ucfirst($batch->status) }}</span></td><td>{{ $batch->created_at->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="4" class="text-center py-5 text-muted">No imports yet.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $batches->links() }}</div></div></div></div></div></div>
@endsection
