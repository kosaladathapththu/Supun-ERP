@extends('layouts.app')
@section('title','Import Preview')
@section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><a href="{{ route('imports.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Import center</a><h1 class="h3 page-title mt-2 mb-1">Import preview</h1><p class="text-muted mb-0">{{ $batch->original_filename }}</p></div>@if($batch->status==='validated')<div class="d-flex gap-2"><form method="POST" action="{{ route('imports.cancel',$batch) }}" onsubmit="return confirm('Cancel this complete import? No live inventory will be changed.')">@csrf<button class="btn btn-outline-danger"><i class="bi bi-x-circle"></i> Cancel Import</button></form><form method="POST" action="{{ route('imports.confirm',$batch) }}" onsubmit="return confirm('Import all validated rows into the ERP?')">@csrf<button class="btn btn-success" @disabled($batch->invalid_rows>0||$batch->valid_rows<1)><i class="bi bi-check2-circle"></i> Confirm Import</button></form></div>@endif</div>
<div class="row g-3 mb-4">@foreach([['Total rows',$batch->total_rows,''],['Valid',$batch->valid_rows,'text-success'],['Invalid',$batch->invalid_rows,'text-danger'],['Status',ucfirst($batch->status),'']] as [$label,$value,$class])<div class="col-md-3"><div class="card"><div class="card-body"><small class="text-muted">{{ $label }}</small><div class="h4 fw-bold mb-0 {{ $class }}">{{ $value }}</div></div></div></div>@endforeach</div>
@if($batch->invalid_rows)<div class="alert alert-danger">Edit or remove every invalid row before confirming the import.</div>@endif
<div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th class="ps-4">Row</th><th>Item code</th><th>Product</th><th>Category</th><th>Quantity</th><th>Cost</th><th>Result</th><th class="text-end pe-4">Actions</th></tr></thead><tbody>@forelse($rows as $row)<tr><td class="ps-4">{{ $row->row_number }}</td><td>{{ $row->data['item_code']??'—' }}</td><td>{{ $row->data['product_name']??'—' }}</td><td>{{ $row->data['category']??'—' }}</td><td>{{ $row->data['quantity']??'0' }}</td><td>{{ $row->data['cost_price']??'—' }}</td><td>@if($row->status==='valid')<span class="badge text-bg-success">Valid</span>@else<span class="badge text-bg-danger">Invalid</span><div class="small text-danger mt-1">{{ implode('; ',$row->errors??[]) }}</div>@endif</td><td class="text-end pe-4"><div class="d-flex justify-content-end gap-1"><a class="btn btn-sm btn-outline-secondary" href="{{ route('imports.rows.show',[$batch,$row]) }}"><i class="bi bi-eye"></i> View</a>@if($batch->status==='validated')<a class="btn btn-sm btn-outline-primary" href="{{ route('imports.rows.edit',[$batch,$row]) }}"><i class="bi bi-pencil"></i> Edit</a><form method="POST" action="{{ route('imports.rows.destroy',[$batch,$row]) }}" onsubmit="return confirm('Remove this product from the pending import?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i> Delete</button></form>@endif</div></td></tr>@empty<tr><td colspan="8" class="text-center text-muted py-5">No product rows remain in this import.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $rows->links() }}</div></div>
@endsection
