@extends('layouts.app') @section('title','Import Preview') @section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><a href="{{ route('imports.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Import center</a><h1 class="h3 page-title mt-2 mb-1">Import preview</h1><p class="text-muted mb-0">{{ $batch->original_filename }}</p></div>@if($batch->status==='validated')<form method="POST" action="{{ route('imports.confirm',$batch) }}" onsubmit="return confirm('Import all validated rows into the ERP?')">@csrf<button class="btn btn-success" @disabled($batch->invalid_rows>0)><i class="bi bi-check2-circle"></i> Confirm import</button></form>@endif</div>
<div class="row g-3 mb-4"><div class="col-md-3"><div class="card"><div class="card-body"><small class="text-muted">Total rows</small><div class="h4 fw-bold mb-0">{{ $batch->total_rows }}</div></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><small class="text-muted">Valid</small><div class="h4 fw-bold text-success mb-0">{{ $batch->valid_rows }}</div></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><small class="text-muted">Invalid</small><div class="h4 fw-bold text-danger mb-0">{{ $batch->invalid_rows }}</div></div></div></div><div class="col-md-3"><div class="card"><div class="card-body"><small class="text-muted">Status</small><div class="h5 fw-bold mb-0">{{ ucfirst($batch->status) }}</div></div></div></div></div>
@if($batch->invalid_rows)<div class="alert alert-danger">Invalid rows are never imported. Correct the listed problems in the source file and upload it again.</div>@endif<div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th class="ps-4">Row</th><th>Item code</th><th>Product</th><th>Category</th><th>Cost</th><th>Result</th></tr></thead><tbody>@foreach($rows as $row)<tr><td class="ps-4">{{ $row->row_number }}</td><td>{{ $row->data['item_code']??'—' }}</td><td>{{ $row->data['product_name']??'—' }}</td><td>{{ $row->data['category']??'—' }}</td><td>{{ $row->data['cost_price']??'—' }}</td><td>@if($row->status==='valid')<span class="badge text-bg-success">Valid</span>@else<span class="badge text-bg-danger">Invalid</span><div class="small text-danger mt-1">{{ implode('; ',$row->errors??[]) }}</div>@endif</td></tr>@endforeach</tbody></table></div><div class="p-3">{{ $rows->links() }}</div></div>
@endsection
