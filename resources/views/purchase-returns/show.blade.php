@extends('layouts.app') @section('title',$return->document_number) @section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><a href="{{ route('purchase-returns.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Purchase returns</a><h1 class="h3 page-title mt-2">{{ $return->document_number }}</h1><p class="text-muted">{{ $return->supplier->name }}  -  GRN {{ $return->grn->document_number }}  -  {{ $return->return_date->format('Y-m-d H:i') }}</p></div><span class="badge text-bg-success">Posted</span></div><div class="row g-4"><div class="col-lg-8"><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Product</th><th>Quantity</th><th>Unit cost</th><th>Reason</th><th class="pe-4 text-end">Total</th></tr></thead><tbody>@foreach($return->items as $i)<tr><td class="ps-4">{{ $i->product->name }}</td><td>@quantity($i->quantity)</td><td>Rs. {{ number_format($i->unit_cost,4) }}</td><td>{{ str($i->reason_code)->headline() }}</td><td class="pe-4 text-end">Rs. {{ number_format($i->line_total,2) }}</td></tr>@endforeach</tbody><tfoot><tr><th colspan="4" class="text-end">Return total</th><th class="pe-4 text-end">Rs. {{ number_format($return->total_amount,2) }}</th></tr></tfoot></table></div></div></div><div class="col-lg-4"><div class="card"><div class="card-body"><h2 class="h5">Debit note</h2><div class="fs-5 fw-semibold">{{ $return->debitNote?->document_number }}</div><div class="mt-2">Amount: Rs. {{ number_format($return->debitNote?->amount,2) }}</div><div>Applied: Rs. {{ number_format($return->debitNote?->applied_amount,2) }}</div><span class="badge text-bg-primary mt-2">{{ str($return->debitNote?->status)->headline() }}</span><hr><strong>Reason</strong><p class="mb-0 text-muted">{{ $return->reason }}</p></div></div></div></div>
@endsection
