@extends('layouts.app') @section('title','Purchase Returns') @section('content')
<div class="mb-4"><h1 class="h3 page-title">Purchase Returns</h1><p class="text-muted">Return received stock to suppliers and issue debit notes.</p></div>
<div class="card mb-4"><div class="card-body"><h2 class="h5">Choose a GRN to return</h2><div class="row g-2 mt-1">@forelse($grns as $g)<div class="col-md-6 col-xl-4"><a class="btn btn-outline-primary w-100 text-start" href="{{ route('purchase-returns.create',$g) }}"><strong>{{ $g->document_number }}</strong><br><small>{{ $g->supplier->name }} · {{ $g->received_date->format('Y-m-d') }}</small></a></div>@empty<div class="text-muted">No posted GRNs are available.</div>@endforelse</div></div></div>
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Return</th><th>GRN</th><th>Supplier</th><th>Date</th><th>Total</th><th>Status</th></tr></thead><tbody>@forelse($returns as $x)<tr><td class="ps-4"><a href="{{ route('purchase-returns.show',$x) }}">{{ $x->document_number }}</a></td><td>{{ $x->grn->document_number }}</td><td>{{ $x->supplier->name }}</td><td>{{ $x->return_date->format('Y-m-d') }}</td><td>Rs. {{ number_format($x->total_amount,2) }}</td><td><span class="badge text-bg-success">{{ str($x->status)->headline() }}</span></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No purchase returns posted yet.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $returns->links() }}</div></div>
@endsection
