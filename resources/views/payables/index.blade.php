@extends('layouts.app') @section('title','Payables') @section('content')
<div class="d-flex justify-content-between mb-4"><div><h1 class="h3 page-title">Supplier Payables</h1><p class="text-muted">Invoices created automatically from posted GRNs.</p></div><div><a href="{{ route('payables.aging') }}" class="btn btn-outline-primary">Aging</a> <a href="{{ route('payables.create') }}" class="btn btn-primary">New Payment</a></div></div><div class="card"><div class="card-body"><table class="table"><thead><tr><th>Invoice</th><th>Supplier</th><th>Date</th><th>Due</th><th class="text-end">Total</th><th class="text-end">Balance</th><th>Status</th></tr></thead><tbody>@forelse($invoices as $x)<tr><td>{{ $x->document_number }}<div class="small text-muted">{{ $x->supplier_invoice_number }}</div></td><td><a href="{{ route('payables.ledger',$x->supplier) }}">{{ $x->supplier->name }}</a></td><td>{{ $x->invoice_date->format('d M Y') }}</td><td>{{ optional($x->due_date)->format('d M Y') }}</td><td class="text-end">{{ number_format($x->total_amount,2) }}</td><td class="text-end">{{ number_format($x->balance_amount,2) }}</td><td>{{ str($x->payment_status)->headline() }}</td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No supplier invoices yet.</td></tr>@endforelse</tbody></table>{{ $invoices->links() }}</div></div>
@endsection
