@extends('layouts.app') @section('title',$delivery->document_number) @section('content')
<div class="d-flex justify-content-between mb-4"><div><a href="{{ route('delivery-notes.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Delivery notes</a><h1 class="h3 page-title mt-2">{{ $delivery->document_number }}</h1><p class="text-muted">{{ $delivery->customer->name }} · {{ $delivery->delivery_date->format('Y-m-d H:i') }}</p></div><button onclick="window.print()" class="btn btn-outline-secondary align-self-start"><i class="bi bi-printer"></i> Print</button></div><div class="row g-4 mb-4"><div class="col-md-4"><div class="card h-100"><div class="card-body"><small class="text-muted">SALES ORDER</small><div><a class="fs-5" href="{{ route('sales-orders.show',$delivery->order) }}">{{ $delivery->order->document_number }}</a></div></div></div></div><div class="col-md-4"><div class="card h-100"><div class="card-body"><small class="text-muted">SALES INVOICE</small><div><a class="fs-5" href="{{ route('sales.show',$delivery->sale) }}">{{ $delivery->sale->document_number }}</a></div></div></div></div><div class="col-md-4"><div class="card h-100"><div class="card-body"><small class="text-muted">DELIVERY ADDRESS</small><div>{{ $delivery->delivery_address ?: 'Not specified' }}</div></div></div></div></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th class="ps-4">Product</th><th class="text-end pe-4">Delivered quantity</th></tr></thead><tbody>@foreach($delivery->items as $i)<tr><td class="ps-4">{{ $i->product->name }}<div class="small text-muted">{{ $i->product->item_code }}</div></td><td class="text-end pe-4">{{ number_format($i->quantity,4) }}</td></tr>@endforeach</tbody></table></div></div>
@endsection
