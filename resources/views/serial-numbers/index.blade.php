@extends('layouts.app')
@section('title','Serial Numbers & Warranty')
@section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><h1 class="h3 page-title mb-1">Serial Numbers & Warranty</h1><p class="text-muted mb-0">Trace electronics from supplier receipt through customer invoice and warranty expiry.</p></div><button class="btn btn-primary" data-bs-toggle="collapse" data-bs-target="#register"><i class="bi bi-plus-lg me-1"></i> Register serials</button></div>
<div class="collapse mb-4" id="register"><div class="card"><div class="card-body"><h2 class="h5">Bulk register serial numbers</h2><p class="text-muted">Optional for wholesale stock. Paste one serial per line or separate them with commas.</p><form method="POST" action="{{ route('serial-numbers.store') }}" class="row g-3">@csrf<div class="col-md-5"><label class="form-label">Serial-tracked product</label><select name="product_id" class="form-select" required><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->item_code }} — {{ $product->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Stock location</label><select name="stock_location_id" class="form-select"><option value="">Not specified</option>@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->code }} — {{ $location->name }}</option>@endforeach</select></div><div class="col-12"><label class="form-label">Serial numbers</label><textarea name="serials" rows="5" class="form-control" required placeholder="SN-00001&#10;SN-00002"></textarea></div><div class="col-12"><button class="btn btn-success">Register serials</button></div></form></div></div></div>
<div class="card"><div class="card-body border-bottom"><form class="row g-2"><div class="col-md-5"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search serial, item code or product"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['available','sold','returned','damaged','service'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-md-auto"><button class="btn btn-outline-primary">Filter</button></div><div class="col-md-auto"><a href="{{ route('serial-numbers.index',['expiring'=>1]) }}" class="btn btn-outline-warning">Expiring in 30 days</a></div></form></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Serial</th><th>Product</th><th>Status</th><th>Supplier / GRN</th><th>Customer / Invoice</th><th>Warranty expiry</th><th></th></tr></thead><tbody>@forelse($serials as $serial)@php($saleItem=$serial->saleItems->first())<tr><td><strong>{{ $serial->serial_number }}</strong></td><td>{{ $serial->product->item_code }}<div class="small text-muted">{{ $serial->product->name }}</div></td><td><span class="badge {{ $serial->status==='sold'?'bg-primary':($serial->status==='available'?'bg-success':'bg-warning text-dark') }}">{{ ucfirst($serial->status) }}</span></td><td>{{ $serial->goodsReceivedNoteItem?->goodsReceivedNote?->supplier?->name ?? 'Not recorded' }}<div class="small text-muted">{{ $serial->goodsReceivedNoteItem?->goodsReceivedNote?->document_number }}</div></td><td>{{ $saleItem?->sale?->customer?->name ?? 'Not sold' }}<div class="small text-muted">{{ $saleItem?->sale?->document_number }}</div></td><td>{{ $serial->warranty_expires_on?->format('d M Y') ?? 'Not started' }}</td><td><a class="btn btn-sm btn-light" href="{{ route('serial-numbers.show',$serial) }}">View</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No serial numbers registered yet.</td></tr>@endforelse</tbody></table></div>@if($serials->hasPages())<div class="card-body">{{ $serials->links() }}</div>@endif</div>
@endsection
