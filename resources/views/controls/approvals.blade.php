@extends('layouts.app') @section('title','Approvals') @section('content')
<div class="mb-4"><a href="{{ route('controls.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Control Center</a><h1 class="h3 page-title mt-2">Approval Queue</h1><p class="text-muted">Four-eyes review history for sensitive control actions.</p></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Type</th><th>Requested by</th><th>Reason</th><th>Status</th><th class="pe-4">Review</th></tr></thead><tbody>@forelse($requests as $x)<tr><td class="ps-4">{{ str($x->approval_type)->headline() }}<div class="small text-muted">{{ $x->created_at->format('Y-m-d H:i') }}</div></td><td>{{ $x->requester->name }}</td><td>{{ $x->reason }}</td><td><span class="badge {{ $x->status==='approved'?'text-bg-success':($x->status==='rejected'?'text-bg-danger':'text-bg-warning') }}">{{ str($x->status)->headline() }}</span>@if($x->reviewer)<div class="small text-muted">{{ $x->reviewer->name }}</div>@endif</td><td class="pe-4">@if($x->status==='pending')<form method="POST" action="{{ route('controls.approvals.review',$x) }}" class="d-flex gap-2">@csrf<input name="review_notes" class="form-control form-control-sm" placeholder="Review notes"><button name="decision" value="approved" class="btn btn-sm btn-success">Approve</button><button name="decision" value="rejected" class="btn btn-sm btn-outline-danger">Reject</button></form>@else{{ $x->review_notes }}@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No approval requests.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $requests->links() }}</div></div>
@endsection
