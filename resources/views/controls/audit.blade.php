@extends('layouts.app') @section('title','Audit Log') @section('content')
<div class="mb-4"><a href="{{ route('controls.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Control Center</a><h1 class="h3 page-title mt-2">Audit Log</h1><p class="text-muted">Append-only record of state-changing web requests. Passwords and CSRF tokens are excluded.</p></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Time</th><th>User</th><th>Action</th><th>Request</th><th>Status</th><th class="pe-4">IP</th></tr></thead><tbody>@forelse($logs as $x)<tr><td class="ps-4">{{ $x->occurred_at->format('Y-m-d H:i:s') }}</td><td>{{ $x->user?->name ?: 'Guest/System' }}</td><td>{{ $x->route_name ?: $x->event }}</td><td><span class="badge text-bg-light">{{ $x->method }}</span> {{ $x->url }}</td><td>{{ $x->response_status }}</td><td class="pe-4">{{ $x->ip_address }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No audited changes yet.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $logs->links() }}</div></div>
@endsection
