@extends('layouts.app') @section('title','Accounting Period Controls') @section('content')
<div class="mb-4"><a href="{{ route('controls.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Control Center</a><h1 class="h3 page-title mt-2">Accounting Period Controls</h1><p class="text-muted">Closing and reopening require a documented approval request.</p></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Period</th><th>Dates</th><th>Status</th><th class="pe-4">Request</th></tr></thead><tbody>@foreach($periods as $p)<tr><td class="ps-4">{{ $p->financialYear->name }} · {{ $p->name }}</td><td>{{ $p->starts_on->format('Y-m-d') }} — {{ $p->ends_on->format('Y-m-d') }}</td><td><span class="badge {{ $p->status==='open'?'text-bg-success':'text-bg-secondary' }}">{{ str($p->status)->headline() }}</span></td><td class="pe-4"><form class="d-flex gap-2" method="POST" action="{{ route('controls.periods.request',$p) }}">@csrf<input type="hidden" name="action" value="{{ $p->status==='open'?'close':'reopen' }}"><input name="reason" class="form-control form-control-sm" placeholder="Required reason" required><button class="btn btn-sm {{ $p->status==='open'?'btn-outline-danger':'btn-outline-success' }}">Request {{ $p->status==='open'?'close':'reopen' }}</button></form></td></tr>@endforeach</tbody></table></div></div>
@endsection
