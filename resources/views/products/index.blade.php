@extends('layouts.app') @section('title','Products') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 page-title mb-1">Products</h1><p class="text-muted mb-0">Electronics catalogue, prices and stock controls.</p></div><a class="btn btn-primary" href="{{ route('products.create') }}"><i class="bi bi-plus-lg"></i> Add Product</a></div><div class="card"><div class="p-3 border-bottom"><form class="d-flex gap-2"><input class="form-control" style="max-width:450px" name="search" value="{{ request('search') }}" placeholder="Search item code, barcode or product"><button class="btn btn-outline-secondary">Search</button></form></div><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th class="ps-4">Item</th><th>Category</th><th>Brand</th><th>Stock</th><th>Cost</th><th>Status</th><th class="text-end pe-4">Action</th></tr></thead><tbody>@forelse($products as $p)<tr><td class="ps-4"><div class="fw-semibold">{{ $p->name }}</div><small class="text-muted">{{ $p->item_code }}{{ $p->barcode?' Â· '.$p->barcode:'' }}</small></td><td>{{ $p->category->name }}</td><td>{{ $p->brand?->name?:'â€”' }}</td><td>@quantity($p->current_quantity) {{ $p->unit->code }}</td><td>Rs. {{ number_format($p->average_cost,2) }}</td><td><span class="badge {{ $p->is_active?'text-bg-success':'text-bg-secondary' }}">{{ $p->is_active?'Active':'Inactive' }}</span></td><td class="text-end pe-4"><a class="btn btn-light btn-sm" href="{{ route('products.edit',$p) }}"><i class="bi bi-pencil"></i> Edit</a></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">No products found. Create categories and units first.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $products->links() }}</div></div>
@endsection
