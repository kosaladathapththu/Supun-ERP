@extends('layouts.app')
@section('title','Expenses')
@section('content')
<div class="d-flex justify-content-between mb-4"><div><h1 class="h3 page-title mb-1">Expense Bills</h1><p class="text-muted mb-0">Track billed, paid, and outstanding operating expenses.</p></div><a href="{{ route('expenses.create') }}" class="btn btn-primary">New Expense Bill</a></div>
<div class="row g-3 mb-4"><div class="col-md-4"><div class="card h-100"><div class="card-body"><div class="text-muted small">Total expense bills</div><div class="h3 mb-0">Rs. {{ number_format((float)$totals->billed,2) }}</div></div></div></div><div class="col-md-4"><div class="card h-100 border-start border-success border-4"><div class="card-body"><div class="text-muted small">Paid</div><div class="h3 mb-0 text-success">Rs. {{ number_format((float)$totals->paid,2) }}</div></div></div></div><div class="col-md-4"><div class="card h-100 border-start border-danger border-4"><div class="card-body"><div class="text-muted small">Not paid yet</div><div class="h3 mb-0 text-danger">Rs. {{ number_format((float)$totals->payable,2) }}</div></div></div></div></div>
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-4">Bill</th><th>Date / Due</th><th>Payee</th><th>Account</th><th class="text-end">Bill amount</th><th class="text-end">Paid</th><th class="text-end">Payable</th><th>Status</th><th class="pe-4"></th></tr></thead><tbody>@forelse($expenses as $expense)<tr><td class="ps-4"><a class="fw-semibold text-decoration-none" href="{{ route('expenses.show',$expense) }}">{{ $expense->document_number }}</a></td><td>{{ $expense->expense_date->format('d M Y') }}<div class="small {{ $expense->due_date&&$expense->due_date->isPast()&&(float)$expense->balance_amount>0?'text-danger':'text-muted' }}">Due: {{ optional($expense->due_date)->format('d M Y')?:'—' }}</div></td><td>{{ $expense->payee }}</td><td>{{ $expense->account->name }}</td><td class="text-end">{{ number_format($expense->amount,2) }}</td><td class="text-end text-success">{{ number_format($expense->paid_amount,2) }}</td><td class="text-end fw-semibold {{ (float)$expense->balance_amount>0?'text-danger':'' }}">{{ number_format($expense->balance_amount,2) }}</td><td><span class="badge text-bg-{{ $expense->payment_status==='paid'?'success':($expense->payment_status==='unpaid'?'danger':'warning') }}">{{ str($expense->payment_status)->headline() }}</span></td><td class="text-end pe-4"><a class="btn btn-sm btn-outline-primary" href="{{ route('expenses.show',$expense) }}">View</a></td></tr>@empty<tr><td colspan="9" class="text-center text-muted py-5">No expense bills posted.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $expenses->links() }}</div></div>
@endsection
