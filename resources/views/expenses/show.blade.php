@extends('layouts.app')
@section('title',$expense->document_number)
@section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><a href="{{ route('expenses.index') }}" class="text-decoration-none"><i class="bi bi-arrow-left"></i> Expense bills</a><h1 class="h3 page-title mt-2 mb-1">{{ $expense->document_number }}</h1><p class="text-muted mb-0">{{ $expense->payee }} · {{ $expense->expense_date->format('d M Y') }}</p></div>@if((float)$expense->balance_amount>0)<a class="btn btn-primary" href="{{ route('expenses.payment',$expense) }}"><i class="bi bi-wallet2 me-1"></i>Pay Expense</a>@endif</div>
<div class="row g-3 mb-4"><div class="col-md-4"><div class="card h-100"><div class="card-body"><div class="text-muted small">Bill amount</div><div class="h3 mb-0">Rs. {{ number_format($expense->amount,2) }}</div></div></div></div><div class="col-md-4"><div class="card h-100 border-start border-success border-4"><div class="card-body"><div class="text-muted small">Paid</div><div class="h3 mb-0 text-success">Rs. {{ number_format($expense->paid_amount,2) }}</div></div></div></div><div class="col-md-4"><div class="card h-100 border-start border-danger border-4"><div class="card-body"><div class="text-muted small">Not paid yet</div><div class="h3 mb-0 text-danger">Rs. {{ number_format($expense->balance_amount,2) }}</div><small class="text-muted">Due {{ optional($expense->due_date)->format('d M Y')?:'date not set' }}</small></div></div></div></div>
<div class="row g-3"><div class="col-lg-4"><div class="card"><div class="card-header bg-white p-3 fw-semibold">Bill details</div><div class="card-body"><dl class="row mb-0"><dt class="col-5">Account</dt><dd class="col-7">{{ $expense->account->code }} — {{ $expense->account->name }}</dd><dt class="col-5">Reference</dt><dd class="col-7">{{ $expense->reference?:'—' }}</dd><dt class="col-5">Status</dt><dd class="col-7">{{ str($expense->payment_status)->headline() }}</dd><dt class="col-5">Description</dt><dd class="col-7">{{ $expense->description }}</dd></dl></div></div></div><div class="col-lg-8"><div class="card"><div class="card-header bg-white p-3 fw-semibold">Payment history</div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th class="ps-3">Payment</th><th>Date</th><th>Method</th><th>Reference</th><th class="text-end pe-3">Amount</th></tr></thead><tbody>@forelse($expense->payments->sortBy('payment_date') as $payment)<tr><td class="ps-3 fw-semibold">{{ $payment->payment_number }}</td><td>{{ $payment->payment_date->format('d M Y') }}</td><td>{{ str($payment->payment_method)->headline() }}</td><td>{{ $payment->reference?:'—' }}</td><td class="text-end pe-3 text-success fw-semibold">{{ number_format($payment->amount,2) }}</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">No payments made yet.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
